# Copyright 2023 Canonical Ltd.
# See LICENSE file for licensing details.

name: mysql-test-app
summary: |
  Test application used in high availability MySQL K8s/VM integration tests.
description: |
  Data platform libs application meant to be used
  only for testing high availability of the MySQL charms.

requires:
  database:
    interface: mysql_client
    limit: 1
  mysql:
    interface: mysql

peers:
  application-peers:
    interface: application-peers
